{
  "name": "dynex-dashboard-pro",
  "version": "2.0.0",
  "private": true,
  "description": "Production-ready DYNEX Discord dashboard with OAuth2, PostgreSQL settings, audit logs and a Sapphire-inspired responsive UI.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js && node --check src/discord.js && node --check src/db.js",
    "test": "node --test"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "axios": "^1.11.0",
    "connect-pg-simple": "^10.0.0",
    "dotenv": "^17.4.2",
    "express": "^4.21.2",
    "express-rate-limit": "^8.3.0",
    "express-session": "^1.18.2",
    "helmet": "^8.1.0",
    "pg": "^8.16.3"
  }
}